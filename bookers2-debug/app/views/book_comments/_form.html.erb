<%= form_with(model: [book, book_comment] ) do |form| %>
  <div class="">
    <%= form.text_area :content, class: "col-xs-12" %>
  </div>
  <div class="actions">
    <%= form.submit "Comment", class: "btn btn-primary col-xs-12" %>
  </div>

<% end %>